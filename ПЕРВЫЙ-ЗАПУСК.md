# Convloo — первый запуск без переменных

Первая версия запускается **без единой переменной окружения**: база —
локальный SQLite-файл, схема создаётся сама, секреты — дев-режим,
внешние API просто молчат (поиск без ключа Google не найдёт лидов,
письма пишутся в лог). Ничего из функций не вырезано — весь код на
месте, переменные добавите при хостинге.

## Запуск (2 терминала)

```bash
# 1. Бэкенд (Python 3.12)
python3.12 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
python -m leadgen                      # API на :8080, БД — ./convloo.db

# 2. Демо-данные (по желанию, чтобы было на что смотреть)
python -m leadgen.scripts.seed_demo

# 3. Фронтенд
cd frontend && npm install && npm run dev   # :3000, сам проксирует на :8080
```

Открыть http://localhost:3000/login

## Демо-логины (пароль `demo1234`)

| Email               | Роль      | Что видно                                   |
|---------------------|-----------|---------------------------------------------|
| sales@demo.local    | селз      | Работа (очередь + прозвон), Входящие, Письма |
| manager@demo.local  | менеджер  | + База, Добыча, Воронки, Аналитика, Команда  |
| admin@demo.local    | админ     | + Настройки (потолок затрат), Коннекторы     |
| owner@demo.local    | владелец  | + Биллинг, передача владения, потолок        |

Без сида продукт стартует пустым (как по спецификации): регистрация →
создание команды → мастер первой воронки.

## Что оживёт после добавления переменных (при хостинге)

Полный список — `.env.example`. Ключевые:

| Переменная | Что включает |
|---|---|
| `DATABASE_URL` | Postgres вместо SQLite (прод; alembic сам накатит схему) |
| `AUTH_JWT_SECRET`, `FERNET_KEY` | боевые секреты сессий и OAuth-хранилища |
| `GOOGLE_PLACES_API_KEY` | реальный поиск лидов (Добыча) |
| `ANTHROPIC_API_KEY` | скоринг, выжимки, заходы, Henry, разбор ответов |
| `REDIS_URL` | воркер arq: авто-письма воронок, дайджесты, эскалации |
| `TELEGRAM_BOT_TOKEN` + `TELEGRAM_WEBHOOK_SECRET` | уведомления событий |
| `YELP_API_KEY`, `FSQ_API_KEY`, `ADZUNA_*` | дополнительные источники |
| `RESEND_API_KEY` / Gmail/Outlook OAuth | реальная отправка почты |
| `NEXT_PUBLIC_API_URL` | адрес API для фронта на отдельном домене |

Проверка живости источников после выдачи ключей:
`RUN_LIVE_SMOKE=1 pytest tests/test_connectors_smoke_live.py -v`
